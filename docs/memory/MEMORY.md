# Memory — Manu Gupta Portfolio

## Entries
- [P1/discovery] DECISION: dropped Medicity's original AI/MERN framing after resume text didn't match repo evidence → reframed around whatever the repo actually shows (kept updating as verification progressed).
- [P3/evidence] MISTAKE: original resume claimed Medicity was React/Node/MongoDB with disease prediction; cloning the actual repo showed a static HTML/CSS/SCSS/jQuery site with a PHP mailer, no backend → RESOLVED: `docs/DISCOVERY.md` and `docs/evidence/PROJECT_SOURCE_NOTES.md` corrected to the verified static-site framing.
- [P3/evidence] GOTCHA: Student Management System repo's root `README.md` describes an unrelated "online job portal" — do not use that README as evidence; the actual source is in `Student_management_system.zip`.
- [resume] MISTAKE: user's original `Manu Resume Edited.pdf` had stale "1st year" education text and "John Doe" PDF metadata (template leftover) → RESOLVED 2026-08-26/29: regenerated clean resume at `assets/resume/Manu_Gupta_Resume.pdf` via `assets/resume/create_resume.py`.
- [resume] MISTAKE: user supplied a second resume draft (2026-08-29) claiming Google Maps/geolocation/symptom-disease-lookup for Medicity and O(1)/Collections-based lookup for Student Management System — re-cloned both repos, confirmed no commits since original verification and no matching code (grepped JS/PHP assets) → RESOLVED: kept resume/portfolio to verified facts only. User's stated plan: build these features into the real repos for real, but only *after* the portfolio ships. Do not add these claims until re-verified as present in code.
- [general] DECISION: user wants gradual, small-commit pacing through 2026-09-15 so GitHub history reads as natural incremental work, not a one-day AI blast. Hold this even when ahead of schedule — see `docs/memory/HANDOFF.md` pacing section.
- [P7/wireframe] MISTAKE: `docs/features/03-project-case-studies.md` and `docs/DISCOVERY.md` §7 listed "Java Collections Framework" as a Student Management System stack tag, contradicting `docs/evidence/PROJECT_SOURCE_NOTES.md`'s explicit "do not claim" list (verified: flat-file I/O, not Collections-backed) → RESOLVED 2026-08-31: both docs and the wireframe corrected to "File I/O". Caught by the Phase 7 wireframe agent cross-checking content sources — a reminder to re-check Phase 3 feature docs against Phase 5 evidence notes whenever both exist.
- [P7/wireframe] DECISION: hero CTA hierarchy — "View projects" = outlined button, "Download resume" = filled Signal Rust primary button — reused consistently in nav/hero/footer.
- [P7/wireframe] MISTAKE: DESIGN.md's primary-button spec paired Signal Rust fill with Bone text — measures ~3.35:1, fails WCAG AA (needs 4.5:1 for 16px/bold, below the 18.66px-bold large-text threshold). Affected the résumé-download CTA in 4 places (nav, hero, mobile menu, footer) plus the skip link. → RESOLVED 2026-08-31: added an "On Accent (Ink)" token (reuses the page background hex, #18160F) for text-on-accent-fill contexts; measures ~4.66:1, passes. DESIGN.md §2 and §5 updated with the rule: Signal Rust as foreground (links/focus rings) pairs with light text as normal; Signal Rust as a fill (buttons) always pairs with Ink text, never Bone. GOTCHA for future palette work: check contrast per role-combination (foreground-on-bg vs fill-with-on-fill-text), not just one direction — same accent hex can pass one role and fail the other.
- [P7/wireframe] MISTAKE: original hero graphic (3 sparse boxes, 2 lines, 2 dots) read as thin/unfinished per direct user feedback ("what is this... want u to replace it") → RESOLVED 2026-09-01: rebuilt as a 6-component blueprint schematic with a faint dot-grid ground and one signal dot that travels a trace once on load (CSS `offset-path`, respects `prefers-reduced-motion`, not a perpetual loop per DESIGN.md §6).
- [P7/wireframe] MISTAKE: all 3 project architecture diagrams (SVG) had text labels wider than their containing boxes — SVG text doesn't wrap, so long labels ("Marksheet.calculateCGPA()", the Medicity page list, "vercel.json root rewrite → ...") overflowed and visually collided with neighboring boxes. Caught by direct user inspection (screenshot), not by the Sonnet reviewer — the review pass didn't catch this. → RESOLVED 2026-09-01: widened boxes/viewBoxes, wrapped longest labels to 2 lines, verified with a headless `getBBox()` measurement of every text element against its box (script-checked, not eyeballed) — zero overflow. GOTCHA: add "measure every SVG text bbox against its container" to the Phase 7/8 diagram review checklist — the Sonnet design-fidelity review didn't think to check this class of bug.
- [P8] DECISION: `docs/IMPLEMENTATION_PLAN.md` was missing the atomic T01-Tn task list the dispatch mechanism (agents.md) requires — only had a narrative 7-step build sequence. Added full T01–T26 breakdown 2026-09-01 (see IMPLEMENTATION_PLAN.md `## Tasks`). Three wireframe-vs-plan mismatches resolved in the task list: no separate `Contact.tsx` (folded into `SiteFooter.tsx`, wireframe never had a separate contact section), collapsed-state project preview icons stay inline in `ProjectCard.tsx` not separate files, and the 3 diagram components must build genuinely-stacked mobile layouts since the wireframe itself only scales the same viewBox down (feature doc forbids this).
- [P8] DECISION: only T01 (Next.js scaffold) executed 2026-09-01 despite T02-T06/T14/T16-T18 being unblocked and parallelizable right after — deliberate, per the hard pacing constraint. Future sessions should dispatch the next unblocked batch, not rush ahead just because dependencies are clear.
- [P8/T01] MISTAKE: orchestrator (not the coder) left scratch verification files (`git status`/`git diff --stat` captures) inside `docs/memory/reviews/` while briefing the T01 reviewer — outside the task's file list, flagged as a "docs/ untouched" violation by the review → RESOLVED: deleted; scratch/debug output belongs in the session scratchpad dir, never inside `docs/`, even when briefing a reviewer.
- [P8/T01] GOTCHA: `create-next-app` refused to run in the non-empty repo root, so the coder scaffolded into a temp dir named `next-scaffold` and merged the new files in — but `package.json`'s `"name"` field inherited that temp dir name. Caught by review, fixed to `"portfolio"`. Any future task that works around the same non-empty-directory refusal must remember to reset `package.json`'s name post-merge.
- [P8/T02] MISTAKE: `docs/IMPLEMENTATION_PLAN.md`'s own "Content model" code sample omitted the `SitePerson` type that `docs/PRD.md` §5 defines as a required entity — the T02 coder followed the plan's sample faithfully and reproduced the gap. → RESOLVED: added `SitePerson` to both `lib/types.ts` and the plan's code sample so future re-reads don't repeat it. GOTCHA: when a plan doc's code sample and the PRD's entity list can drift, the PRD is authoritative — cross-check both, not just the more convenient one.
- [P8/T03] MISTAKE: three real bugs in the design-token wiring, all fixed and re-verified live (not just edited-and-trusted): (1) Tailwind v4 `@theme inline` block used `--colors-*` (plural) instead of the required singular `--color-*` — silently produced zero color utility classes, no build error; (2) `globals.css` font tokens hardcoded literal strings (`'Red Hat Display'`) instead of referencing next/font's self-hosted generated CSS vars (`--font-red-hat-display` etc. from app/layout.tsx) — fonts downloaded but never applied, silent fallback to system-ui; (3) `scroll-margin-top` used `--touch-target-min` (44px) instead of `--space-8` (96px), which would land anchor-nav targets partially behind the future sticky header. → RESOLVED: fixed all three, then re-verified by temporarily injecting a test element using the color utilities into app/page.tsx, rebuilding, and grepping the actual compiled CSS output for the expected hex values and font-var chain — not just re-reading the source. Test element removed before commit. GOTCHA for future @theme/token work: a wrong Tailwind v4 theme key name fails silently (no error, just no utility class) — verify by testing a real utility class against compiled output, don't just eyeball the block.
- [P8] GOTCHA: the `nextjs-dev` specialist agent self-commits its work directly (T03 as 212fcae, T05 and T06 similarly) before the orchestrator's review-then-commit step runs — unlike the general-purpose+haiku coders used for T01/T04, which left work uncommitted. Check `git log` for a task's files before assuming its output is uncommitted; fixes after a failed review on a self-committed task become a new follow-up commit, not a duplicate "T0N:" commit.
- [P8/T06] MISTAKE: `content/profile.ts`'s bio field used straight apostrophes (') where `docs/features/02-about.md`'s source text uses curly apostrophes (', U+2019) — 2 instances, likely an editor auto-normalization when typing the TS string literal. Caught by the reviewer diffing actual codepoints, not eyeballing. → RESOLVED: fixed both, verified by counting U+2019 occurrences directly. GOTCHA: "verbatim" checks on copied prose need a codepoint-level diff for quote/dash style, not just a visual read — add this to the standing verbatim-content checklist alongside URL character-matching.
- [P8/T05] MISTAKE: `SectionHeading.tsx` reimplemented the wireframe's `.section-label`/`.section-heading`/`.section-intro` styling from scratch instead of copying wireframe/index.html's exact property values — every value used a legitimate design token (no raw hex/px), but several were the *wrong* token: label was missing font-family/font-weight (mono kicker style) and used the wrong spacing token; heading was missing font-weight/letter-spacing; intro used `--color-text` instead of `--color-text-muted`. Slips past a naive "uses var(--...)" check. → RESOLVED: matched wireframe/index.html:264-283 property-for-property. Since this is the shared heading component every T07+ section reuses, catching it here prevented it propagating everywhere. GOTCHA: reviewing a wireframe-port component needs a property-by-property diff against the actual wireframe source, not just confirming tokens are used somewhere.
- [P8] GOTCHA: `docs/IMPLEMENTATION_PLAN.md`'s task-list Status/Progress edits can end up uncommitted alongside a task's own commit if the orchestrator edits the plan after a self-committing specialist already committed its files — check `git status` on the plan file itself when wrapping up any task, not just the task's declared files.
- [P8/T14] MISTAKE: `content/projects.ts` had 4 plausible-sounding but unverified capability claims that slipped past a stack-tag/keyword grep: Medicity `oneLiner` said "appointment booking" (a static site can't "book" anything — implies a data flow the source notes explicitly say isn't there, and Medicity already had one MERN/AI framing mistake); job-portal outcome said seekers "track status" and problem said "search and apply" — neither itemized in `PROJECT_SOURCE_NOTES.md`'s verified-workflow lists. → RESOLVED 2026-09-01: reworded all four to only what's verified. GOTCHA: content-accuracy review must check every capability verb (booking, tracking, searching) against the source notes' literal lists, not just run keyword/stack-tag greps — plausible inferred functionality is the failure mode grep-based checks miss.
- [P8/T16-T18] MISTAKE: all 3 diagram components had the same systemic bug — both the desktop and mobile `<svg>` are always rendered (CSS `display` toggle, not conditional rendering), but each pair of `<title>` elements shared one `id`, referenced by `aria-labelledby` on both SVGs. Duplicate IDs are invalid HTML; `aria-labelledby` resolution against a dupe is undefined across browsers/AT. → RESOLVED 2026-09-01: unique `-desktop`/`-mobile` suffixed ids on all three components. GOTCHA: whenever a component renders parallel desktop/mobile variants toggled by pure CSS (not conditional JSX), both trees exist in the DOM simultaneously — check for duplicate ids/labels across them, not just that "a mobile version exists."
- [P8/T16] MISTAKE: `StudentManagementDiagram.tsx` had `'use client'` for a styled-jsx CSS media query with zero runtime state (no useState/useEffect/handlers) — directly contradicts `IMPLEMENTATION_PLAN.md`'s "Component responsibility boundaries" table, which marks diagram components non-client. Also had a stray empty `<text>` node and an inconsistent hardcoded `fontSize="10"` override on one label. → RESOLVED: removed `'use client'`, removed the dead node, removed the font-size override (default token-driven 11px fits fine). GOTCHA: a coder justifying `'use client'` by naming styled-jsx/CSS-only needs should be cross-checked against the plan's component-boundary table before accepting — CSS-only responsiveness never needs it.
- [P8/T18] MISTAKE: `JobPortalDiagram.tsx` used one shared `<svg viewBox="0 0 680 300">` with desktop/mobile `<g>` groups toggled by CSS `display`, instead of two separate `<svg>` elements each sized to its own content (the working pattern already established by `MedicityDiagram.tsx`/`StudentManagementDiagram.tsx` in the same batch). SVG clips to its viewBox by default — the mobile group's stacked content ran to y=432 inside a 300-tall viewBox, so the Roles/audit box (required content) was entirely invisible on mobile, MySQL box partially clipped. Build and tsc both passed clean since this is a visual/layout bug, not a compile error. → RESOLVED 2026-09-01: rewrote to the two-svg-per-breakpoint pattern, each viewBox sized to its own content with margin. GOTCHA: when a single-SVG dual-layout pattern is used, always check the mobile group's max Y/X against the viewBox bounds by hand — a reviewer/coder confirming "the mobile group has different coordinates" isn't enough, the coordinates also have to fit.
- [P8/T10] MISTAKE: `SiteFooter.tsx` h2 heading used a straight apostrophe (U+0027) instead of the wireframe's curly one (U+2019) in "Let's build something thoughtful." — same recurring failure mode as T06's bio apostrophes. → RESOLVED. Third occurrence of this exact bug class (T06, now T10) — worth having coder prompts explicitly call out "match apostrophe/dash codepoints exactly" for any task copying wireframe prose into a string literal, not just relying on review to catch it every time.
- [P8/T13] MISTAKE: `Achievements.tsx`'s `<section>` omitted `className="section-pad"` present in the wireframe and both sibling sections (About, Skills) — would have collapsed vertical spacing between sections once wired into page.tsx by T20. → RESOLVED. GOTCHA: reviewing a wireframe-port section component must check the top-level `<section>` element's class list against sibling sections, not just inner content styling — a missing structural spacing class is easy to miss when focused on content fidelity.
- [P8/T20] MISTAKE: two real bugs surfaced only once T20 wired every section together for the first time (both invisible to any prior isolated task's build/tsc checks): (1) `ProjectCard.tsx` rendered each project's name as a plain `<span class="project-title">` instead of a heading — with `<h2>Projects</h2>` as the section heading and `<h4>` for Problem/Architecture/etc. inside `ProjectDetail`, this skipped a level (h2→h4, no h3), breaking screen-reader heading navigation; (2) mounting all 3 diagram components' `<style jsx>` blocks simultaneously (only happens once Projects.tsx is actually wired into the page) produced a reproducible React "missing key prop" console warning in dev mode — confirmed absent from the actual production build (React strips dev-only warnings), but still worth fixing properly. → RESOLVED 2026-09-01: promoted `project-title` to `<h3>` (fixing the hierarchy: h2→h3→h4, verified via `curl | grep '<h[1-6]'` on live rendered HTML); migrated all 3 diagrams' `<style jsx>` blocks into `app/globals.css` as static classes (renaming Student Management's generic `.diagram-svg-desktop/mobile` to `.sms-diagram-desktop/mobile` to avoid colliding with the other two diagrams now that all three mount on the same page) and removed `'use client'` from all three — this also restores them to proper server components, resolving the earlier justified-exception note in IMPLEMENTATION_PLAN.md. Verified: console clean on a fresh dev reload, tsc clean, build clean. GOTCHA: some classes of bug (heading-hierarchy-across-components, multi-instance library quirks) are structurally invisible until the real page composition task runs — a "some earlier task might need revisiting" mindset is warranted whenever a convergence task like T20 fails review, not just fixing the convergence task's own files.
- [P8/T19] MISTAKE: `Projects.tsx` passed `label="Case Studies"` to `SectionHeading` — invented copy not present anywhere in `wireframe/index.html` (that section has no `.section-label` kicker, unlike About). → RESOLVED: dropped the label prop. Slug→diagram mapping itself (the highest-risk part of this task) verified correct by tracing actual imports/keys: student-management→StudentManagementDiagram, medicity→MedicityDiagram, job-portal→JobPortalDiagram. GOTCHA: "matches wireframe copy" means literally present in wireframe/index.html, not just plausible/on-theme invented text — this is now the second/third time a coder added on-brand-sounding but unsourced copy.
- [P8/T20] CORRECTION: initially suspected T20's addition of `'use client'` to all 3 diagram components was an incorrect band-aid (diagrams were built and reviewed clean as server components back in T16-T18) — was wrong, tested empirically by reverting it and running a clean build, which genuinely failed: `Error: 'styled-jsx' cannot be imported from a Server Component module`. Root cause: during T16-T18, these diagram files were never actually imported anywhere in the app (page.tsx only rendered Hero), so `next build`'s bundler never enforced the Server/Client boundary on them — they were orphan files that happened to type-check. Only once T20 wired Projects.tsx (which imports them) into app/page.tsx did the real constraint surface. GOTCHA: `npx tsc --noEmit` (used as a supplementary check specifically because `next build` doesn't type-check unimported files) does NOT catch Next.js Server/Client Component boundary violations either — that's a bundler-enforced React Server Components rule, not a TypeScript error, and it only triggers once a component is actually reachable from an entry point. Neither check alone is sufficient for a component that isn't wired in yet; a "PASS" on an orphan component's build/tsc check is provisional, not final, until something actually imports it. IMPLEMENTATION_PLAN.md's component-boundary table (which marks diagrams "No" for client) has a justified, tested exception for these 3 components specifically because they use `<style jsx>` — noted in the plan file.
- [P8] GOTCHA: dispatching 6 parallel nextjs-dev coder agents (T09/T10/T11/T12/T13/T15) against the same working tree, each independently running `npm run build`/`npx`, caused real filesystem contention — not just flaky timing. One agent's build fell back to `npx`, which pulled a *different* Next.js version (16.3.4) than the pinned 16.3.3 because a native SWC binary got corrupted mid-download from concurrent npm cache writes, forcing a webpack fallback instead of Turbopack. Another agent reported explicit "package-lock.json corruption." Two agents left stray scratch log files (`build_full.log`, `build_output.txt`) at the repo root instead of the scratchpad. → RESOLVED: full `node_modules`+`.next` wipe and clean reinstall, verified with a fresh sequential `npm run build` (confirms Next 16.3.3/Turbopack, matching `package.json`) plus a project-wide `npx tsc --noEmit` (which — unlike a build whose import graph doesn't reach unwired components yet — actually type-checks every `.tsx` file regardless of whether `page.tsx` imports it). GOTCHA for future batches: large parallel batches of coder agents that each run npm install/build against a shared working tree risk real corruption, not just review-time flakiness — after any such batch, do one clean sequential repair-and-verify pass before trusting any individual agent's "build passed" claim, and don't rely on `npm run build` alone to catch type errors in components nothing imports yet.
- [P8/T22] MISTAKE: the T22 coder's first pass generated `public/og-image.png` with `font-family="Arial, sans-serif"` in the raw SVG, but the final report claimed "Font: Red Hat Display (weights 500, 600, 700, 800)" — a false claim about the actual output, caught only by reading the generation script directly rather than trusting the report. → RESOLVED: sent back to the same agent, which fetched the real Red Hat Display woff2 files from Google Fonts' CSS2 API, embedded them as base64 `@font-face` rules inside the SVG's `<defs>`, and re-rendered — verified independently by re-reading the regenerated PNG and visually confirming the letterforms actually changed (rounder "a", flat-terminal "G" vs. Arial's narrower shapes), not just re-trusting the second report either. GOTCHA: a coder's plain-English summary of "what font/asset/token was used" is a claim like any other — for anything visual/generated (images, fonts, rendered output), verify by reading the actual generated artifact or its source, not just the report prose, exactly the same discipline already applied to code claims.
